@model IEnumerable<GroupProject1_272.Models.Collector>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Collector_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Collector_Surname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Collector_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Collector_Surname)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Collector_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Collector_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Collector_ID })
        </td>
    </tr>
}

</table>
