@model IEnumerable<GroupProject1_272.Models.Donation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Donation_Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Donor.Donor_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Donation_Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Donor.Donor_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Donation_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Donation_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Donation_ID })
        </td>
    </tr>
}

</table>
